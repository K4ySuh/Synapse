# Synapse Version Log (local, gitignored)

Private implementation history for this local clone. **This file is not
committed** — it is your own scratch log. Copy this template to
`docs/Version-Log.md` (setup does this automatically via `bin/check-setup` if the
file is missing) and keep your own dated notes here. Reverse chronological.

Record every code change you make: what changed, why, how it was verified, and
whether it was committed. When a set of changes ships, summarize the shared,
user-facing narrative into the committed `CHANGELOG.md` at the repo root.

<!-- Copy the block below for each new entry. Newest entry on top. -->

## YYYY-MM-DD — Short title

- What changed and where (module/file), in operational terms.
- Why it matters (the behavior it fixes or the capability it adds).
- How it was verified (tests run, `bin/test` result, any manual/e2e checks).
- Commit status (committed as <hash>, or "not yet committed").
