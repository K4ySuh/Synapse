interface:
  display_name: "Operate Synapse"
  short_description: "Route and operate authorized Synapse work"
  default_prompt: "Use $operate-synapse to recover the workspace and choose the smallest safe direct or specialist workflow."

dependencies:
  tools:
    - type: "mcp"
      value: "synapse"
      description: "Local Synapse MCP server for controlled assessment operations."
