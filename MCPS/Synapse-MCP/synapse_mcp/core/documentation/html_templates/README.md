# HTML Report Templates

This directory stores standalone HTML report templates and mockups used by the
Synapse documentation layer.

Keep templates local-first and self-contained before wiring them into generated
reports. Generated report code should continue to live in
`core/documentation/`; adapters should emit workspace entities and evidence
rather than rendering report sections directly.
